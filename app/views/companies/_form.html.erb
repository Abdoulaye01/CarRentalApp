<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :company_city %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :company_address %>
  </div>

  <div class="field">
    <%= form.label :areacode %>
    <%= form.text_field :areacode, id: :company_areacode %>
  </div>

  <div class="field">
    <%= form.label :phoneNumber %>
    <%= form.number_field :phoneNumber, id: :company_phoneNumber %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
